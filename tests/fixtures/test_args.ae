// test_args.ae — test that main() receives argv[1] as string parameter
import "std/test"

@test
func main(inputString: string): void {
    print("hello {inputString}\n")
    assertTrue(true, "args test compiled and ran")
}
