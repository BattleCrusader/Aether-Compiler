// test_interp_multi.ae — multiple interpolations in one string
import "std/test"

@test
func main(): void {
    let a = "hello"
    let b = "world"
    let c = "!"
    let msg = "{a} {b}{c}"
    print(msg)
}
