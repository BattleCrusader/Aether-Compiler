// test_std_test.ae — verify std/test.ae works
import "std/test"

@test
func main() {
    assertEquals(2 + 3, 5, "2 + 3 should be 5")
    assertEquals(10 - 4, 6, "10 - 4 should be 6")
    assertTrue(true, "true should be true")
    assertEquals(3 * 4, 12, "3 * 4 should be 12")
}
