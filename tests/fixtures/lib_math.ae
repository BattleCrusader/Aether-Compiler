// lib_math.ae — A simple math library for testing .aelib format

pub func add(a: u64, b: u64): u64 {
    return a + b
}

pub func multiply(a: u64, b: u64): u64 {
    return a * b
}

func private_helper(x: u64): u64 {
    return x * 2
}
