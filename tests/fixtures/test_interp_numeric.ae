// test_interp_numeric.ae — numeric-to-string via + concat
import "std/test"

@test
func main(): void {
    let n: u64 = 42
    let msg = "value: " + n
    print(msg)
    assertEquals(42, n, "n == 42")
}
