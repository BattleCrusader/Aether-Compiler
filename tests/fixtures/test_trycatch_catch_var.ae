// test_trycatch_catch_var.ae — catch with variable binding
func do_throw(): u64 throws {
    throw 42
    return 0
}

@test(expect=42)
func main(): u64 {
    let mut result: u64 = 0

    try {
        result = do_throw()
    } catch Error(e) {
        result = e
    }

    return result
}
