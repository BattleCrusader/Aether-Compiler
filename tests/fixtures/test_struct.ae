// test_struct.ae — struct declaration, field access
import "std/test"

struct Point {
    x: int
    y: int
}

@test
func main(): void {
    let p: Point
    // Can't construct struct literals yet, but struct types compile
}