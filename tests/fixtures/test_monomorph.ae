// test_monomorph.ae — generics monomorphization
import "std/test"

func identity<T>(value: T): T {
    return value
}

@test
func main(): void {
    let x = identity(42)
    assertEquals(42, x, "test_monomorph.ae")
}
