// test_enum.ae — test enum declaration, layout, and construction
import "std/test"

enum Option {
    Some(int)
    None
}

@test
func main(): void {
    let x: Option = Option::Some(42)
    let y: Option = Option::None
    assertTrue(true, "enum compiled and ran")
}
