// test_sysfunc.ae — syscall page function declaration
import "std/test"

sys func putc(c: byte) at(0)
sys func puts(s: string) at(1)

@test
func main(): void {
    assertTrue(true, "sysfunc compiled and ran")
}
