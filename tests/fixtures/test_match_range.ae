// test_match_range.ae — test match range patterns
func classify(x: u64): u64 {
    match x {
        case 1..5 -> 10
        case 6..10 -> 20
        case _ -> 0
    }
}

func main(): u64 {
    let result = classify(3)
    return result
}
