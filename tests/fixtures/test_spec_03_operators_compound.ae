// test_spec_03_operators_compound.ae — Section 3.4: Compound Assignment
// Tests: compound operators via explicit reassignment
import "std/test"

@test
func main(): void {
    let x: u64 = 10
    let y: u64 = x + 5
}
