// test_contract.ae — contract programming with pre/post conditions
import "std/test"

func divide(a: int, b: int): int pre(b != 0) {
    return a / b
}

@test
func main(): void {
    assertTrue(divide(6, 2) == 3, "divide(6, 2) == 3")
}
