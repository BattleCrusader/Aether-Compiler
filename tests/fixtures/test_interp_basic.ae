// test_interp_basic.ae — basic string interpolation with variables
import "std/test"

@test
func main(): void {
    let name = "World"
    let msg = "Hello {name}!"
    print(msg)
    assertTrue(true, "basic interpolation compiled and ran")
}
