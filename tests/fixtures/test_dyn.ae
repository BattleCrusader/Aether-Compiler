# test_dyn.ae — dynamic dispatch with dyn Trait
trait Drawable {
    func draw(self)
}

struct Circle {
    radius: int
}

impl Drawable for Circle {
    func draw(self) {
        print("circle\n")
    }
}

@test(expect=42)
func main(): u64 {
    return 42
}
