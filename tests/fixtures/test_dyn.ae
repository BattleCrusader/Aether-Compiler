// test_dyn.ae — dynamic dispatch with dyn Trait
import "std/test"

trait Drawable {
    func draw(self)
}

struct Circle {
    radius: int
}

impl Drawable for Circle {
    func draw(self) {
        print("circle\n")
    }
}

@test
func main(): void {
}
