# test_dyn.ae — dynamic dispatch with dyn Trait
# @expected(42)
trait Drawable {
    func draw(self)
}

struct Circle {
    radius: int
}

impl Drawable for Circle {
    func draw(self) {
        print("circle\n")
    }
}

func main(): u64 {
    return 42
}
