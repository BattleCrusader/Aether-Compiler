// test_export.ae — @export attribute for kernel modules
import "std/test"

@export func mod_init(): int {
    return 0
}

@test
func main(): void {
    assertTrue(mod_init() == 0, "mod_init returns 0")
}
