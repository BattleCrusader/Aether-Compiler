// test_null_concat.ae — test that string concat handles null pointers
import "std/test"

@test
func main(): void {
    // Test 1: concat with null (no args passed = null string)
    print("hello \n")
    assertTrue(true, "null concat compiled and ran")
}
