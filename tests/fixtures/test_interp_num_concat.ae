// test_interp_num_concat.ae — number + string and string + number
import "std/test"

@test
func main(): void {
    let n: u64 = 42
    let a = n + " is the answer"
    print(a)
    let b = "the answer is " + n
    print(b)
}
