# test_catch_all.ae — catch-all with _
func do_throw(): u64 throws {
    throw 99
    return 0
}

@test(expect=42)
func main(): u64 {
    let mut result: u64 = 0

    try {
        result = do_throw()
    } catch _ {
        result = 42
    }

    return result
}
