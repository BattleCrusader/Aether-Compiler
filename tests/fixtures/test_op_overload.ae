// test_op_overload.ae — operator overloading
import "std/test"

struct Vector {
    x: int
    y: int
}

func op_add(a: int, b: int): int {
    return a + b
}

@test
func main(): void {
    assertTrue(op_add(2, 3) == 5, "op_add(2, 3) == 5")
}
