# test_trycatch_finally.ae — try/catch with finally block
@test(expect=42)
func main(): u64 {
    let mut result: u64 = 0
    let mut finally_ran: u64 = 0

    try {
        result = 42
    } catch Error {
        result = 0
    } finally {
        finally_ran = 1
    }

    return result
}
