// test_default_param.ae — test default function parameters
func greet(name: string, greeting: string = "Hello"): string {
    return greeting ++ " " ++ name
}

func main(): u64 {
    let msg = greet("World")
    print(msg)
    print("\n")
    return 0
}
