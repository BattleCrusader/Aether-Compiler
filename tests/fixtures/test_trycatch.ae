// test_trycatch.ae — basic try/catch/throw
import "std/test"

@test
func main(): void {
    let mut result: u64 = 0

    try {
        result = 42
    } catch Error {
        result = 0
    }

    assertEquals(42, result, "test_trycatch.ae")
}
