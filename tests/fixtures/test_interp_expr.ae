// test_interp_expr.ae — interpolation with numeric expressions
import "std/test"

@test
func main(): void {
    let x: u64 = 40
    let y: u64 = 2
    let msg = "sum = {x + y}"
    print(msg)
    assertEquals(42, x + y, "x + y == 42")
}
